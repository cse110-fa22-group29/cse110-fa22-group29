{
  "name": "food-journal",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --recursive './{,!(node_modules)/**}/*.test.js'",
    "lint": "eslint '**/*.js'",
    "fix-style": "eslint --fix **/*.js"
  },
   "html": {
    "lintHTML": "npx htmlhint '**/*.html'",
  },
  "devDependencies": {
    "eslint": "^8.27.0",
    "mocha": "10"
  }
}
